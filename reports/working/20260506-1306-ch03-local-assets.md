# 作業完了レポート

保存先: `reports/working/20260506-1306-ch03-local-assets.md`

## 1. 受けた指示

- asset も各ディレクトリで管理するようにする。

## 2. 要件整理

| 要件ID | 指示・要件 | 重要度 | 対応状況 |
|---|---|---:|---|
| R1 | CH03 構造配下の図版 asset を該当ディレクトリへ配置する | 高 | 対応 |
| R2 | TeX 本文の asset 参照をローカル配置に合わせる | 高 | 対応 |
| R3 | README から asset を確認できるようにする | 中 | 対応 |

## 3. 検討・判断したこと

- CH03 構造配下の本文では `fig-ch03-01.png` から `fig-ch03-12.png` までの 12 図を参照していた。
- 各図は、それを参照する TeX ファイルと同じディレクトリの `assets/` に置く方針とした。
- 既存の共有 asset は削除せず、構造配下で管理するためのコピーを追加した。
- 構造配下の TeX は現行 master ビルド入力ではないため、参照を `assets/<file>.png` に変更した。

## 4. 実施した作業

- 各図を参照元ディレクトリの `assets/` 配下へコピーした。
- TeX 内の `assets/generated_figures/ch03/<file>.png` 参照を `assets/<file>.png` に変更した。
- asset を持つ各 `README.md` に `## Assets` セクションを追加した。
- CH03 構造配下に旧 `assets/generated_figures/ch03` 参照が残っていないことを確認した。
- CH03 構造配下のローカル PNG が 12 個であることを確認した。

## 5. 成果物

| 成果物 | 形式 | 内容 | 指示との対応 |
|---|---|---|---|
| `*/assets/fig-ch03-*.png` | PNG | 各節・小節ディレクトリで管理する図版 | asset のローカル管理に対応 |
| `content.tex` など | TeX | ローカル `assets/` 参照へ更新 | asset 参照整合に対応 |
| `README.md` updates | Markdown | `## Assets` と図版リンクを追加 | 検収容易性に対応 |
| `reports/working/20260506-1306-ch03-local-assets.md` | Markdown | 追加作業レポート | レポート要件に対応 |

## 6. 指示へのfit評価

| 評価軸 | 評価 | 理由 |
|---|---:|---|
| 指示網羅性 | 5 | 参照している CH03 図版をすべて該当ディレクトリへ配置した |
| 制約遵守 | 5 | 既存共有 asset は壊さず、構造配下の管理単位を追加した |
| 成果物品質 | 5 | TeX 参照と README リンクをローカル asset に合わせた |
| 説明責任 | 5 | 対象図数、配置方針、確認内容を記録した |

総合fit: 5.0 / 5.0（約100%）
理由: CH03 構造配下の asset 参照を各ディレクトリ管理へ移し、確認可能なリンクも追加した。

## 7. 確認内容

- `rg -n "assets/generated_figures/ch03" 'tex/swebok_structure/CH03.ソフトウェア設計(Software_Design)' || true`: 該当なし。
- `find 'tex/swebok_structure/CH03.ソフトウェア設計(Software_Design)' -path '*/assets/*.png' | wc -l`: `12`。

## 8. 未対応・制約・リスク

- 今回の変更は構造配下の asset 管理変更で、現行 TeX ビルド入力には影響しないため LuaLaTeX の再ビルドは未実施。
